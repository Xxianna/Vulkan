# 构建过程记录

## 环境

| 项目 | 值 |
|------|------|
| 项目路径 | `C:\prj\vkapp\Vulkan` |
| Vulkan SDK | `C:\VulkanSDK\1.4.350.0` |
| 编译器 | MSVC 19.51.36244.0 (VS18 Community) |
| CMake | 4.2.3-msvc3 |
| 构建生成器 | Ninja |
| C++ 标准 | C++20 |

## 构建步骤

### 1. 初始化子模块

```powershell
cd C:\prj\vkapp\Vulkan
$env:HTTP_PROXY='http://127.0.0.1:7897'
$env:HTTPS_PROXY='http://127.0.0.1:7897'
git submodule update --init --recursive external/glm
git submodule update --init --recursive assets
```

子模块：
- `external/glm` — glm 数学库 (commit `1ad55c50`)
- `assets` — 纹理/模型资源 (commit `a27c0e58`, main 分支)

### 2. CMake 配置

```
cmake -G "Ninja" -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl ..
```

CMake 自动找到 `C:/VulkanSDK/1.4.350.0/Lib/vulkan-1.lib`。

### 3. 编译

```
cmake --build . --parallel
```

所有 205 个目标编译成功，仅有一个无关警告（`descriptorbuffer.cpp` 中 `VkDeviceSize` 转 `uint32_t`）。

## 注意事项

- **VS 版本**：本机安装的是 VS18（非 VS17/2022），不能用 `Visual Studio 17 2022` 生成器，改用 Ninja + cl。
- **glm/gli 等子模块**：初始为空，必须通过代理拉取。
- **imgui**：`external/imgui/` 目录中只有核心文件，缺少 `backends/`（`imgui_impl_vulkan`、`imgui_impl_win32` 等），imgui example 可能无法编译。
- **gli 子模块**：`.gitmodules` 中未定义 gli 子模块，需确认 gli 内容是否完整。

## 输出路径

可执行文件位于 `C:\prj\vkapp\Vulkan\build\bin\` 目录下。

---

# Android 构建过程

## 环境

| 项目 | 值 |
|------|------|
| 项目路径 | `C:\prj\vkapp\Vulkan\android` |
| Android SDK | `C:\Users\u\AppData\Local\Android\Sdk` |
| Android NDK | `30.0.14904198`（位于 `Sdk\ndk\30.0.14904198`） |
| Gradle | 8.13（wrapper 自动下载） |
| AGP | 8.12.3 |
| JDK | Android Studio JBR (`C:\Program Files\Android\Android Studio\jbr`) |
| ABI | `arm64-v8a` |
| minSdk | 21 |
| targetSdk / compileSdk | 33 |
| C++ 标准 | C++20 |
| 代理 | `http://127.0.0.1:7897` |

## 构建方法

项目使用 **Gradle** 作为 Android 构建工具（不是直接 CMake，CMake 由 Gradle 通过 `externalNativeBuild` 调用）。

### 1. 创建 `local.properties`

```
sdk.dir=C\:\\Users\\u\\AppData\\Local\\Android\\Sdk
```

（`ndk.dir` 已废弃，改用 `android.ndkVersion` 控制版本）

### 2. 修改根 `build.gradle`

原始项目的 `compileSdkVersion = 26`、`targetSdkVersion = 26` 太旧，AGP 8.12.3 会默认选择 `ndkVersion = "27.0.12077973"`，而本机未安装该版本。

修改内容（`android/build.gradle`）：

**a) 更新 SDK 版本和 NDK 版本：**

```groovy
ext {
    abiFilters = "arm64-v8a"
    minSdkVersion = 21
    targetSdkVersion = 33          // 原 26
    compileSdkVersion = 33         // 原 26
    ndkVersion = "30.0.14904198"  // 新增，指定本机已安装的 NDK
    shaderPath = '../../../shaders/'
    assetPath = '../../../assets/'
}
```

**b) 强制所有子项目使用统一 NDK 版本和 release 签名：**

```groovy
subprojects {
    plugins.withId('com.android.application') {
        android {
            ndkVersion rootProject.ext.ndkVersion
            if (namespace == null) {
                namespace "de.saschawillems." + project.name
            }
            buildTypes {
                release {
                    signingConfig signingConfigs.debug
                }
            }
        }
    }
}
```

- `ndkVersion` 必须在 `plugins.withId` 回调中设置，否则 AGP 默认选择 27.0.x 版本导致版本冲突
- `signingConfig signingConfigs.debug` 使 release APK 可用 debug keystore 签名，安装到设备

### 3. 构建命令

```powershell
$env:HTTP_PROXY='http://127.0.0.1:7897'
$env:HTTPS_PROXY='http://127.0.0.1:7897'
$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
cd C:\prj\vkapp\Vulkan\android
.\gradlew.bat assembleRelease --no-parallel
```

**注意：不能使用 `--parallel`。** 所有 98 个子项目共享同一个 `android/examples/base/` 目录编译 `libbase` 和 `libktx` 静态库，并行时多个 ninja 同时向 `base/CMakeFiles/` 写入同一 `.o` / `.d` 文件，导致 `Permission denied` 冲突。必须 `--no-parallel` 顺序构建。

### 4. 输出

APK 输出到各子项目的 `build/outputs/apk/release/` 目录，文件名格式为 `<项目名>-release.apk`。
`outputfilename.gradle` 同时将所有 APK 复制到 `C:\prj\vkapp\Vulkan\bin\` 目录。

### 5. 编译警告（可忽略）

- `strtok` 已弃用 → `VulkanDebug.cpp:115`，建议改用 `strtok_r()`
- `_json` 字面量操作符 → `json.hpp`，C++20 格式变更
- Java source/target 版本 8 已弃用 → AGP 默认行为，不影响功能

### 6. 注意事项

- **Java 环境**：`JAVA_HOME` 必须指向 Android Studio 自带的 JBR，系统 PATH 中无 java
- **着色器**：构建前需确保 GLSL 着色器已编译为 `.spv` 文件（`copyTask` 在构建时自动复制）
- **gli 子模块**：未定义，但 Android 编译不需要（仅 Windows/Linux 桌面版使用）
- **代理**：Gradle 首次运行需通过代理下载依赖
